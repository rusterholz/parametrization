require 'spec_helper'

describe Parametrization do
  it 'has a version number' do
    expect(Parametrization::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
